// Billing utilities for metering and invoicing
export function recordUsage(taskId: string, amount: number) {
  // Placeholder: record usage details
  console.log(`Recording ${amount} for task ${taskId}`)
}
